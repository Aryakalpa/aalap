export default function SplashScreen({ onComplete }) {
  // This is handled by main logic usually, but here is a simple component
  return <div style={{height: '100vh', display: 'grid', placeItems: 'center', background: '#000'}}>Starting Aalap...</div>;
}